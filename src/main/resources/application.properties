
# Servera iestatījumi
server.port=8080
server.address=0.0.0.0

spring.datasource.url=jdbc:postgresql://localhost:5432/warehouse_db
spring.datasource.username=warehouse_user
spring.datasource.password=parole123
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.flyway.enabled=true
spring.flyway.url=jdbc:postgresql://localhost:5432/warehouse_db
spring.flyway.user=warehouse_user
spring.flyway.password=parole123
spring.jpa.hibernate.ddl-auto=none

spring.cache.type=simple


spring.flyway.schemas=public

spring.flyway.locations=classpath:db/migration

spring.flyway.baseline-on-migrate=true

spring.liquibase.enabled=false
spring.main.web-application-type=servlet

logging.level.org.flywaydb=DEBUG
logging.level.org.springframework.boot.context.embedded=DEBUG

spring.jpa.database=postgresql
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

logging.level.org.springframework=DEBUG
logging.level.org.hibernate=DEBUG

#TOmcat
# Set maximum number of threads for Tomcat (default is 200)
server.tomcat.threads.max=200

# Set minimum number of spare threads for Tomcat (default is 10)
server.tomcat.threads.min-spare=10

# Set the maximum size of HTTP requests (default is 2MB)
server.tomcat.max-swallow-size=2097152
server.tomcat.max-http-form-post-size=2097152

management.endpoints.web.exposure.include=*


